{{- if .Values.configMap }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: gateway-migration-env-cm
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-30"
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
{{- toYaml .Values.configMap.migrationEnv | nindent 2 }}
{{- end }}
