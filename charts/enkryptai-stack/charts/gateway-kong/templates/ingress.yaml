{{- if .Values.ingress.enabled -}}
{{- $globalDomain := .Values.global.domain | default "" -}}
{{- $autoHost := "" -}}
{{- if $globalDomain }}
{{- $autoHost = printf "api.%s" $globalDomain -}}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "gateway-kong.fullname" . }}
  labels:
    {{- include "gateway-kong.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if or .Values.ingress.tls (and $autoHost .Values.ingress.tls) }}
  tls:
    {{- if .Values.ingress.tls }}
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
    {{- else if $autoHost }}
    - hosts:
        - {{ $autoHost | quote }}
      secretName: gateway-kong-tls
    {{- end }}
  {{- end }}
  rules:
    {{- if .Values.ingress.hosts }}
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "gateway-kong.fullname" $ }}
                port:
                  number: {{ $.Values.service.ports.http.port }}
          {{- end }}
    {{- end }}
    {{- else if $autoHost }}
    - host: {{ $autoHost | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "gateway-kong.fullname" . }}
                port:
                  number: {{ .Values.service.ports.http.port }}
    {{- end }}
{{- end }}
