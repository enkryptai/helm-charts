{{- if .Values.secrets.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: onprem
type: Opaque
stringData:
  {{- range $key, $val := .Values.secrets.onprem }}
  {{- if ne $key "enabled" }}
  {{ $key }}: {{ $val | default "" | toString | quote }}  {{- end }}
  {{- end }}
{{- end }}
